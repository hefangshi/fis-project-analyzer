{
    "name": "fis-project-analyzer",
    "version": "0.0.28",
    "main": "index.js",
    "description": "A tool to analyze fis-plus project deps",
    "keywords": ["fis", "dependency", "analyzer"],
    "scripts": {
        "test": "make test-all"
    },
    "config": {
        "blanket": {
            "pattern": "fis-project-analyzer/lib"
        },
        "travis-cov": {
            "threshold": 90
        }        
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/hefangshi/fis-project-analyzer.git"
    },
    "engines": {
        "node": ">= 0.8.0"
    },
    "dependencies": {
        "fis-kernel" : "2.0.0"
    },
    "devDependencies": {
        "mocha": "*",
        "blanket": "*",
        "travis-cov": "*",
        "coveralls": "*",
        "mocha-lcov-reporter": "*"
    }
}
